# 🔒 Regression Lock Log — Global Vibez DSG

**Purpose:** Every permanent bug fix gets a row in this file. When a new agent
forks this job with a clean context, they read this file FIRST so they don't
accidentally reintroduce a bug we've already squashed.

**Rule:** If you fix a bug, you MUST:
1. Add a row to the table below
2. Add a pytest case in `/app/backend/tests/regression_shield.py`
3. Confirm `/app/scripts/run_shield.sh` passes

No exceptions. This is how we stop the whack-a-mole.

---

## Lock Log

| Date | Bug | Root cause | Fix | Test ID in regression_shield.py |
|---|---|---|---|---|
| 2026-02-02 | Hearts pass-modal cards invisible | `text-slate-100` ink on white card face | Dropped to `text-slate-900` + `text-rose-700`, bumped font sizes | `test_hearts_pass_modal_ink_is_dark` |
| 2026-02-02 | Euchre hand hidden during bidding | `<SpadesHandFan>` gated to `phase === "playing"` only | Render for all non-finished phases with `isYourTurn=false` in non-play | `test_euchre_hand_visible_during_bidding` |
| 2026-02-02 | Pinochle hand hidden during bidding | Same root cause as Euchre | Same fix pattern | `test_pinochle_hand_visible_during_bidding` |
| 2026-02-02 | Gin Rummy cards not grouped by meld | `to_view()` didn't return meld metadata | Added `meld_groups` aggregate + per-card `meld_id` to backend response | `test_gin_rummy_to_view_exposes_meld_groups` |
| 2026-02-02 | Rummy cards not grouped by meld | Same as Gin Rummy | Same fix pattern | `test_rummy_to_view_exposes_meld_groups` |
| 2026-02-02 | `/baccarat` canonical route 404'd | Route not registered — only `/baccarat-premium` existed | Added `/baccarat` + `/baccarat-aaa` routes | `test_baccarat_has_canonical_route` |
| 2026-02-02 | Baccarat showed `$` USD instead of `₵` | Legacy currency copy | Audited all `$` label strings, replaced with `₵` | `test_baccarat_uses_vibez_coins_not_usd` |
| 2026-02-02 | Fresh demo user had 0 credits → Baccarat errored | No seed on `/auth/demo-login` user creation | Added `"credits_balance": 5000` seed + auto top-up for legacy demo users | `test_demo_login_seeds_credits` |
| 2026-02-02 | Frontend Docker build OOM-killed (3 failed deploys) | No `NODE_OPTIONS` in Dockerfile — Node's default 1.5 GB heap too small for 925+ module webpack | Added `ENV NODE_OPTIONS="--max-old-space-size=4096"` to Dockerfile + inline prefix in build script | `test_dockerfile_has_node_heap_bump` |
| 2026-02-02 | Privy modal rendered huge on prod domain | `auth.privy.io`'s CSP blocks framing from domains not in Privy dashboard | Wrapped `usePrivy()` in ErrorBoundary + 4s timeout + `securitypolicyviolation` listener → button self-hides | `test_privy_button_has_self_hide_guard` |
| 2026-02-03 | Whole login card glowed on touch — users read that as "app is broken, I can't log in" | `GlassCard` wrapping login form inherited `hoverable=true` default | Passed `hoverable={false}` on login card + moved the glow to each action button (Google/Demo/Sign In) individually | `test_login_card_not_globally_hoverable` |
| 2026-02-03 | Vibe 6-5-4 rooms had no spectator tipping / hype / side-bet layer — Founder PDFs were unshipped | No `vibe_654_social` router or matching frontend overlays | Added `routes/vibe_654_social.py` (tips, hype, side-bets, odds, settlement hook) + shared `components/vibe654/Coliseum.tsx` prototype + `VibeColiseum` / `VibeSoloHighRoller` pages | `test_vibe_654_social_routes_mounted`, `test_vibe_654_coliseum_component_exists`, `test_vibe_654_hype_fee_is_one_coin`, `test_vibe_654_sidebet_settlement_hook_wired`, `test_vibe_654_solo_high_roller_route_registered` |
| 2026-02-04 | Solo High Roller Ante-In returned HTTP 402 for legacy demo users — entire Solo loop blocked | `/api/vibez-654/start` only checked `token_balance`, but demo accounts carry `credits_balance` | Wallet-field fallback: read both keys, use whichever is populated. Mirror fix on payout/loss settlement | `test_vibez_654_solo_accepts_credits_balance_wallet` |
| 2026-02-04 | Legacy `/http-multiplayer/<game>` + `/multiplayer-<game>` URLs bounced to `/` instead of AAA canonical rooms | `HttpGameRouter` is mounted at a different path (`/http-multiplayer-game/:gameType/:gameId`), so router-level redirects didn't match legacy bookmarks | Added 11 top-level `<Route>` Navigate aliases in `gamesRoutes.tsx` | `test_stale_http_multiplayer_card_games_redirect` |
| 2026-02-04 | Euchre + Pinochle had no live multiplayer — AAA rooms were AI-only | Missing router + lobby + room page | Added `routes/card_multiplayer.py` (10 endpoints), `CardMpLobbyModal`, `CardMpRoomPage`, "Play Live Multiplayer" CTA on both AAA lobbies | `test_card_multiplayer_routes_mounted`, `test_card_mp_room_page_and_routes_exist`, `test_euchre_pinochle_have_multiplayer_cta` |
| 2026-02-04 | SocketIO services lost type hints (24 functions across bid_whist, messaging, matchmaking, base, connect4) | Historical code was untyped | Annotated every handler `(sid: str, data: dict) -> None`; `register_xxx(sio: object) -> None` | `test_socketio_handlers_have_type_hints` |
| 2026-02-04 | Standalone Vibe Dice 654 (`/dice`) returned "Insufficient balance" for demo users with 96k credits | `/play` endpoint locked onto the FIRST wallet candidate found — a stray 0-balance `wallets` row — instead of the populated `users.credits_balance` field | Replaced the if/elif chain with a `candidates` list sorted by balance (descending) so the wallet with the most funds wins | `test_vibe654_play_picks_highest_balance_wallet` |
| 2026-02-04 | Solo Vault decision pop-down obscured the dice tray + qualifier chips by sitting in the middle of the screen | A single full-screen overlay was used for every viewport | Built `SideDockDecision` — a fixed right-edge dock on `lg` and a bottom-sticky bar on mobile/tablet | `test_solo_vault_decision_dock_is_side_mounted` |
| 2026-02-04 | Every game room shared the same generic top-bar look — Founder asked for "different view style to each room" | No themable shared bar component existed | Added `RoomMenuBar` with 19 colour themes (spades, hearts, vibe654, vibesolo, etc.) and mobile-friendly truncation | `test_room_menu_bar_component_with_themes` |
| 2026-02-04 | 13-card Spades hand fan overflowed the 390px iPhone viewport (~600px wide at desktop sizing) | `SpadesHandFan` hardcoded `size="md"` + `marginLeft: -28` overlap | Added viewport listener + dynamic overlap calc + `cardSize="sm"` switch below 640px | `test_spades_hand_fan_scales_on_mobile` |
| 2026-02-04 | AAA top bars (Spades/Hearts/Pinochle/etc.) crammed Back-button + 4 chips + score badge onto one row → clipped under 640px | Containers used `flex items-start justify-between` with no wrap | Bulk-applied `flex-wrap` to all 12 AAA top bars + `order-3 w-full sm:order-none` on the pills column so it drops to a new line on mobile | `test_aaa_top_bars_wrap_on_mobile` |

---

## Things that are LOCKED and MUST NEVER CHANGE without an explicit user instruction

- **Solana network = `devnet`** in `backend/.env`. Do NOT switch to `mainnet-beta` without user saying the safe phrase `"project complete"`.
- **Resend sender = `onboarding@resend.dev`** (test sender). Do NOT switch to `noreply@globalvibezdsg.com` without the same safe phrase.
- **Currency displayed = `₵` (Vibez Coins)** everywhere player-facing. Never `$` USD.
- **Brand name = "Global Vibez DSG"**, never "Social Connect - 953" (the preview slug confuses the user).
- **Coming Soon gating** — `/app/frontend/src/data/comingSoonGames.ts` is the single source of truth. 32 IDs currently gated. Never edit a consumer (lobby, practice page, HTTP router) without updating this file.
- **Privy app ID = `cmof0ab0b00mj0ckzhthh8x8o`** (user's Privy dashboard). Never hardcode a different one.

---

## How to run the shield

```bash
# Quick — runs in ~15 seconds
/app/scripts/run_shield.sh

# Full — also runs frontend TypeScript check
cd /app/backend && python -m pytest tests/regression_shield.py -v
cd /app/frontend && npx tsc --noEmit --skipLibCheck
```

If ANYTHING in the shield fails → DO NOT DEPLOY. Fix it first.
| 2026-02-05 | Card-suit grouping changed from C-H-S-D to **H ♥ → C ♣ → D ♦ → S ♠** per Founder request | Old SUIT_ORDER constant put clubs first | Flipped order (still alternating red/black for visual separation) | `test_hand_fan_sorts_by_suit` (now also asserts numeric ordering h<c<d<s) |
| 2026-02-05 | Vibe rooms required vertical scroll while betting/playing — Founder asked for "more compressed" layouts | All rooms used `min-h-screen` (grow vertically) so chip rail + dice + decision dock spilled below the fold on phones | Converted Solo Vault, /dice, and Coliseum to a `h-[100dvh] flex flex-col overflow-hidden` shell with internal `flex-1 min-h-0 overflow-y-auto` main region. Bet panels collapse via drawer toggles. | `test_vibe_rooms_use_compressed_single_viewport_layout` |
| 2026-02-05 | Coliseum center stage was a static Trophy + pot label — Founder asked for live qualifier progress with a different palette than Solo Vault | No live qualifier visualisation existed for the multiplayer arena | Rebuilt center stage with cyan-emerald qualifier chips (6/5/4 lit by `roundResult.hit_654`), 5 tiny dice spots showing the last final score, and a cyan-emerald "Play Next Orbit" button (distinct from Solo Vault's amber-fuchsia) | `test_coliseum_center_stage_shows_live_qualifier_chips` |
| 2026-02-05 | Standalone /dice room had stacked Balance + Side Bets + Recent Rolls panels pushing the chip rail off-screen | All sidebar panels rendered always-visible | Side Bets + Recent Rolls moved behind floating drawer toggles. Balance + Top-Up moved into the room menu bar's `rightSlot`. | `test_vibedice_premium_drawer_toggles_present` |
| 2026-02-06 | Multi-human voice/video missing for 2-20 player rooms | No signaling endpoint or Focus System UI existed | Implemented `routes/vibe_room_signaling.py` (`/api/vibe-room/ws/{room_id}/{user_id}` + `/api/vibe-room/{room_id}/peers`) with `peer_list / peer_joined / peer_left / rtc_signal / speaker_update` events + 20-peer cap. Frontend `VibeRoomVoice.tsx` ships the Focus System (≤4 = overlay-grid, >4 = sidebar-grid; audio always-on for everyone, video focused on top-N speakers). Wired into the Coliseum. | `test_universal_2_to_20_player_signaling_endpoint_present`, `test_vibe_room_signaling.py` (4 e2e tests) |
| 2026-02-06 | 16+ legacy `HttpMultiplayer*` rooms had bespoke headers — no unified visual language | Each page wrote its own `<h1>` + back button | Created `HttpRoomShell.tsx` themable wrapper; `HttpGameRouter` now wraps every render with it. Per-game theme map covers chess/connect4/poker/blackjack/etc. Dominoes redirects to AAA `/dominoes`. | `test_http_room_shell_unifies_legacy_multiplayer_pages` |
| 2026-02-06 | Privy SDK threw `TypeError: e is not a function` + CSP frame-ancestors 403 on every preview URL because Emergent's dynamic preview hostnames cannot be whitelisted in Privy Dashboard | SDK was always mounted regardless of host | Added `PRIVY_SKIP_PATTERNS` host regex list — skips Privy on `*.preview.emergentagent.com` + localhost. `?force_privy=1` query string overrides for QA. | `test_privy_auth_skips_preview_domains` |
| 2026-02-06 | Coliseum compressed center stage was only verified via source-grep — could regress at runtime | No e2e probe existed | Added `test_coliseum_e2e_probe.py` Playwright test that creates a real table via `/api/vibe654/tournament/create-table` and asserts qualifier chips + tiny dice + pot + voice bar in the DOM. Skips silently when Chromium isn't installed. | `test_coliseum_e2e_probe_file_present` |
| 2026-02-06 (R3) | VibeRoomVoice rendered placeholder letter-avatars instead of real video tiles | WebRTC negotiation was deferred during the initial signaling-only build | Full rewrite: RTCPeerConnection per peer, glare-avoidance via `userId < remoteId`, STUN servers `stun.l.google.com:19302`, real `<video srcObject>` tiles when stream has live video tracks, audio-always-on via hidden `<audio>` tags for off-focus peers | `test_vibe_room_voice_does_real_webrtc_negotiation` |
| 2026-02-06 (R3) | 24 `HttpMultiplayer*` pages still shipped a bespoke `<motion.h1>♔ TITLE ♚</motion.h1>` block above the new shared `RoomMenuBar` — double headers | Cleanup wasn't done when `HttpRoomShell` was added | Bulk-stripped the redundant title block via Python regex; bumped `min-h-screen` → `min-h-[calc(100dvh-56px)]` to fill the area below the menu bar | `test_http_multiplayer_pages_drop_redundant_inline_headers` |
| 2026-02-06 (R3) | Dominoes chain tiles stayed at scale=1.0 even with 12+ tiles → board overflowed mobile | Master Plan v2 PDF specified dynamic scaling but it wasn't implemented | Applied `Math.max(0.4, 0.7 - chain.length * 0.01)` to the motion.div animate prop + added `data-tile-scale` testid attribute | `test_dominoes_chain_uses_master_plan_dynamic_tile_scale` |
| 2026-02-06 (R3) | Multi-human signaling under concurrent load wasn't verified | Only 2-peer tests existed | Added `test_three_concurrent_peers_room` (TestClient) + `test_multi_human_qa_voice.py` (Playwright async, skips without Chromium) + manual checklist at `/app/memory/MULTI_HUMAN_QA_CHECKLIST.md` | `test_three_concurrent_peers_room` |
| 2026-02-06 (R4) | Symmetric-NAT + carrier-NAT testers couldn't establish WebRTC because no TURN relay was wired | Only STUN servers were configured | Added OpenRelay TURN fallback (free public relay) when `REACT_APP_TURN_URL` is unset; reads private TURN credentials from env triplet otherwise | `test_vibe_room_voice_has_turn_fallback_and_ptt` |
| 2026-02-06 (R4) | No Push-to-Talk in VibeRoomVoice — open-mic only | PTT mode wasn't built | Added spacebar PTT hotkey with chat-composer guard (`isTypingTarget`); UI shows PTT toggle button with emerald glow when pressed; mic button disables when PTT is active to prevent conflict | `test_vibe_room_voice_has_turn_fallback_and_ptt` |
| 2026-02-06 (R4) | 42 shadcn/ui primitives were `.jsx` while the rest of the codebase was `.tsx` — inconsistent | Initial scaffold used .jsx | Bulk-renamed all 42 files to `.tsx`. CRA picks them up automatically (extension resolution order). 0 .jsx files remain. | `test_no_jsx_files_remain_in_frontend` |
| 2026-02-06 (R5) | Vibe-Ridez `driver/me` + Vibe-Drive `status` returned 401 against the demo Bearer token | `get_current_user_from_session` + `_resolve_user_id` only checked the `session_token` cookie | Added `Authorization: Bearer ...` fallback to both auth dependencies | `test_auth_dependency_accepts_bearer_header` |
| 2026-02-06 (R5) | No Power Hour gate on chair purchases (Master Plan v4) | Feature wasn't built | New `/api/power-hour/status` + server-side `is_power_hour_active()` (5pm-9pm America/New_York) + chair purchase pipeline stamps `power_hour_bonus=True` and multiplies `weight` by `POWER_HOUR_MULT=1.10` | `test_power_hour_master_plan_v4_endpoint_present`, `test_power_hour_badge_frontend_component_present` |
| 2026-02-06 (R5) | No Sponsor Achievement bonus (Master Plan v4) | Feature wasn't built | New `/api/sponsors/{link,verify,me,leaderboard}`. 5 verified sponsors → 1 free chair, idempotent on `sponsor_ach::{ambassador_id}::{n}` key, lifetime cap of 3 free chairs / 15 sponsors per ambassador. Mirror onto `chair_purchases` + bump `profit_share_counters`. | `test_sponsor_achievement_v4_endpoint_present` + `test_sponsor_link_then_admin_verify_grants_free_chair` |
| 2026-02-06 (R5) | No vote-skip in Vibe-Drive (Spotify ladder step 1) | Feature wasn't built | New `POST /api/vibe-drive/vote-skip` — simple-majority threshold, idempotent upsert on `(ride_id, user_id, track_uri)`, audit collection `vibe_drive_skip_votes` | `test_vibe_drive_vote_skip_endpoint_present` |
| 2026-02-06 (R6) | No Spotify Auto-DJ — Master Plan ladder step 2 not built | Feature wasn't built | `POST /api/vibe-drive/auto-dj/seed` builds queue via `sp.recommendations()` seeded from rider's last-5 played + driver vibe genres; `GET /auto-dj/queue/{ride_id}` reads back. Helpers added to `services/spotify_service.py`. | `test_vibe_drive_auto_dj_endpoints_present` |
| 2026-02-06 (R6) | No Tip-to-Skip / Tip-to-Add — Spotify ladder step 3 | Feature wasn't built | `POST /api/vibe-drive/tip-skip` (100 ₵) + `/tip-add` (50 ₵), both routed through Sovereign Engine `process_transaction(tx_type=VIBE_DRIVE_TIP)` so 30% tax + 70% post-tax to driver | `test_vibe_drive_tip_skip_and_tip_add_endpoints_present` |
| 2026-02-06 (R6) | No Sovereign Engine — v9 PDF supply/tax constants weren't enforced anywhere | Feature wasn't built | `services/sovereign_engine.py` (TOTAL_SUPPLY_CAP=3B, SOVEREIGN_TAX=13.5%, VIBERIDEZ_TAX=30%, RIDE_SPLIT=70%, AMBASSADOR_DIVIDEND=2%) + `routes/sovereign_engine_routes.py` with `/api/economy/status` + `/api/engine/process-transaction` | `test_sovereign_engine_economy_status_endpoint_present` |
| 2026-02-06 (R6) | No 3D Chair Hall · Infinity Table | Feature wasn't built | `pages/ChairHall.tsx` Three.js scene at `/chair-hall` — central glass plinth + economy ring + chair orbs ringed by phase color; reads `/economy/status` + `/chairs/wall` | `test_chair_hall_infinity_table_page_present` |
| 2026-02-06 (R7) | No Solana Bridge execution path (v11 PDF) | Not built | `sovereign_ops_routes.py` with 5 endpoints: `/bridge/request`, `/admin/bridge/queue`, `/admin/bridge/{id}/approve`, `/admin/bridge/{id}/broadcast`, `/admin/bridge/{id}/reject`. Pending→approved→broadcast OR pending→rejected (coins refunded) lifecycle. Dry-run default; live SPL mint gated to 403 until safe phrase. | `test_sovereign_ops_routes_mounted` |
| 2026-02-06 (R7) | No 12-month Inactivity Reap (v11 PDF) | Not built | `/admin/inactivity/candidates` + `/admin/inactivity/run`. `INACTIVITY_DAYS=365`, BOTH `last_login_at` AND `last_activity_at` must be stale. Live mode is allowed (off-chain ₵), 50% → `giveaway_fund_state`, 50% → `leadership_dividends_queue`. Idempotent on `(user_id, cutoff_iso)`. | `test_sovereign_ops_routes_mounted` |
| 2026-02-06 (R7) | No AI Governor Burn-Slide (v12 PDF) | Not built | `services/ai_governor.py`: `burn_rate = min(0.05, max(0, (supply - 350M) / 50M * 0.01))` + anti-whale caps (2M std / 5M chair / crew exempt) + 5× chair mining multiplier. Endpoints `/burn/schedule` (public) + `/admin/burn/execute` (dry-run default, live 403). | `test_v12_ai_governor_burn_slide_formula` |
| 2026-02-06 (R7) | Admin POST returns 500 because Motor's `insert_one` mutated the dict and added ObjectId | Response model can't serialize bson.ObjectId | `doc.pop("_id", None)` before returning. Added to ObjectId-leak guard assertion in the regression shield. | `test_sovereign_ops_routes_mounted` (asserts `pop("_id", None)`) |
| 2026-02-06 (R9.1) | Card-game payouts bypassed Sovereign Tax — winners got full pre-tax gross credited | PDF-E violation across Spades/BW/V654 | `services/card_game_payouts.py::settle_taxable_payout` routes every payout through `sovereign_engine.process_transaction` + credits only NET; Vibez 654 taxes net winnings (not bet refund) | `test_card_game_payouts_wire_sovereign_tax` |
| 2026-02-06 (R9.1) | No turn-timer watchdog — a disconnected player could freeze a table forever | PDF-B Turn Latency | New `POST /api/turn-timer/check` (15s) + `stamp_turn_start` + `pick_lowest_card` helpers + stamping wired in BW/Spades play_card | `test_turn_timer_route_and_stamp_wired` |

