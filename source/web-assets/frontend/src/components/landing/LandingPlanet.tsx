/**
 * LandingPlanet — landing-hero planet entry point.
 *
 * The production look is the hub continent globe (orbital rings, tap-to-open
 * hubs, fiery DSG satellite trail) implemented in LandingOrbitGlobe — not a
 * bare R3F distort sphere. Keep this file as the stable import name.
 */
export { default, default as LandingPlanet } from "./LandingOrbitGlobe";
