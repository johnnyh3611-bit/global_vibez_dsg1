{
  "$schema": "https://railway.app/railway.schema.json",
  "_comment": "Guard config only. Do not deploy this directory directly. Use service-specific railway.json files in backend/ and frontend/ subdirectories. See RAILWAY_DEPLOY.md.",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "bash -lc 'echo \"ERROR: Wrong Railway target. Set Root Directory to source/web-assets/backend or source/web-assets/frontend.\" && exit 1'",
    "restartPolicyType": "NEVER"
  }
}
