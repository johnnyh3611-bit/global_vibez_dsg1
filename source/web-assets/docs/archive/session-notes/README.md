# Archived session notes

Historical agent / sprint markdown (`*COMPLETE*`, `*REPORT*`, handoff dumps, etc.).

**These files are not the source of truth.** Prefer:

| Doc | Purpose |
|-----|---------|
| `/SHIP_CORE.md` (repo root) | Definition of complete + fine-tune rules |
| `/README.md` (repo root) | How to run & deploy |
| `/PRODUCTION_OPS.md` (repo root) | Live www + API wiring |
| `../../README.md` | App-level pointers |

Do not add new “COMPLETE” status docs here — update `SHIP_CORE.md` or open a PR description instead.
