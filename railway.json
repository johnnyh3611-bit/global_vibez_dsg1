{
  "$schema": "https://railway.app/railway.schema.json",
  "_comment": "Guard config: do not deploy repo root to Railway. Create two services (backend-api → source/web-assets/backend, frontend-globalvibez → source/web-assets/frontend). Railway does not support a services[] array in one railway.json — see source/web-assets/RAILWAY_DEPLOY.md.",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "bash -lc 'echo \"ERROR: Wrong Railway target. Use source/web-assets/backend or source/web-assets/frontend as the service Root Directory.\" && exit 1'",
    "restartPolicyType": "NEVER"
  }
}